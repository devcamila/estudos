/*
--COMENTÁRIOS--
JavaScript (// ou /** / )
html <!-- -->
css /* * /


---------------------------------------------
--VARIÁVEIS E TIPOS PRIMITIVOS--
Identificadores(nomeVariavel): começa com letra, $ ou _
Sempre usar espaço entre operadores e após a virgula
Indentação: use dois espaços
Sempre termine uma insttrução simples com ;
Tipos de variáveis= string, number, boolean, null, undefined, symbol


null(vazio) !== undefinded(indefinido)


Template Strings: `Olá ${nome}!`
Space holder: ${}

-----------------------------------------------
--PRECEDÊNCIA DE OPERAÇÕES--
() 
**
* / %
+ -
% : mode
------------------------------------------------
--CONDICIONAIS--

--IF / ELSE--
if (condição) {
    ação ;
} else if (condição) {
    ação ;
} else {
    ação ;
}

--SWITCH--

switch (condição) {
    case 'A' :
        ação ;
            break;
    case 'B' :
        ação ;
            break;
     case 'C' :
        ação ;
            break;
    default :
        ação;
            break;
}

----------------------------------------------------
--ESTRUTURAS DE REPETIÇÂO--

for (let i = 0; i <= 100; i++) {
    ação quando houver
}

while (condição) {     : não sei o fim, é preciso criar uma flag de parada
    ação
}

-----------------------------------------------------
--FUNÇÔES--

function nomeDaFuncao(parametro) {
    instruções
    return
}

nomeDaFuncao(parametro)

-------------------------------------------------------
--CLASSES--
maniera de criar objetos e lidar com herança

class nomeCLasse {
    funçãoA(parametro) {
        return
    }
    funçãoB(parametro) {
        return
    }
}

Criar instância:
var criarInstância = new nomeClasse() ;


*/
